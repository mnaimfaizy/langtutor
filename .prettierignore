# Sandcastle runtime dirs (pnpm store, logs, worktrees)
.sandcastle/.pnpm-store
.sandcastle/logs
.sandcastle/worktrees

# Build output & deps
node_modules
.next
out
build
coverage

# Generated / lockfiles
pnpm-lock.yaml
next-env.d.ts
supabase/.temp

# Hand-authored long-form docs (avoid reflowing tables)
PLAN.md
English Learning Web App Guide.md
# Skill docs with `path/*` globs — Prettier Markdown treats `*` as emphasis and
# rewrites e.g. `llm/*` → `llm/_` (issue #108).
.claude/skills/**/SKILL.md
